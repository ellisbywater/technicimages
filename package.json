{
  "name": "vue-image-server",
  "version": "1.0.0",
  "description": "server for vue-image",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ebwx",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
